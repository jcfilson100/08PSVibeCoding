## CONTEXT
## Context, The Internal Tool Nobody Uses

**User voice:**
- "It's faster to keep my deals in a spreadsheet than to fight the CRM's eight required fields." (, Account exec, 2 logins / month)
- "Logging activity feels like data entry for management, not something that helps me sell." (, Senior AE)
- "If it could just tell me who to call next, I'd open it every morning." (, SDR, occasional user)

**Metrics:**
- 18%, Weekly active adoption across licensed seats.
- 11 min, Avg. task time for "update a deal" vs. 2-min target.
- 2.1 / 5, Internal CSAT for the tool.

## REFERENCE
Match this sales dashboard to better assist the salespeople. (Screenshot attached)

## CONSTRAINTS
Reduce the required fields to under 8
Task time for updating a deal should be lower than 11 minutes and trending downward through a 6 month period
Dashboard clearly shows what's next actions for salespeople

## OUTPUT
Rep home screen — "Next Best Action"
   - On login, surface a prioritized call/follow-up list (not a blank pipeline view).
   - Solves: SDR quote ("If it could just tell me who to call next, I'd open it every morning").

2. Streamlined deal-update flow
   - Cut required fields from 8 to 3 max (e.g., stage, next step, close date — everything else optional or auto-filled).
   - Target: update-a-deal task completes in ≤2 minutes (vs. current 11 min).
   - Solves: AE quote about the spreadsheet being faster than the CRM's required fields.

3. Passive activity logging
   - Auto-capture calls/emails/meetings from connected tools instead of manual entry where possible.
   - Manual logging, where still required, should take <30 seconds.
   - Solves: Senior AE quote ("Logging activity feels like data entry for management").

4. Admin/leadership adoption panel (separate, role-gated view — not shown to reps)
   - Live weekly active adoption %, trended month over month.
   - Visual status against two thresholds: 50% floor (kill-switch — flag red if not trending +5pts/60 days) and 60% healthy target.
   - This is the only screen that should reference the cancellation/kill-switch logic — keep it out of the rep-facing UI.

5. Visual/layout reference
   - Match the attached dashboard screenshot's [layout / component style / information density — specify which] for the rep home screen.
   - Confirm screenshot is actually attached before submitting.